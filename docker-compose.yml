services:
  jekyll:
    image: jekyll/jekyll
    volumes:
      - .:/srv/jekyll
      - ./vendor/bundle/:/usr/local/bundle
    ports:
      - "4000:4000"
    working_dir: /srv/jekyll/docs
    command: jekyll serve --watch --incremental --config _config.yml --host 0.0.0.0